# TrancheReady (Part 1 — Foundation)

Production-ready Node/Express foundation for Render Web Service.

## Quick start (local)
```bash
cp .env.example .env
npm i
npm start
# open http://localhost:10000
